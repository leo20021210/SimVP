name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - pip
  - python
  - pytorch
  - pytorch::torchvision
  - pip:
    - tqdm
    - scikit-image
prefix: /opt/anaconda3/envs/simvp